# URL da API Go (usada só no servidor Nuxt)
API_URL=http://localhost:8080
# Segredo/token para autenticar com a API Go (nunca exposto ao cliente)
API_SECRET=
# Feature flag pública; a exportação LGPD fica indisponível por padrão
NUXT_PUBLIC_LGPD_EXPORT_ENABLED=false
